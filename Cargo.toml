[workspace]
members = [
    "Libraries/LibJS/Rust",
]
exclude = [
    "Libraries/LibJS/AsmIntGen",
]
resolver = "2"

[profile.release]
panic = "abort"

[profile.distribution]
inherits = "release"
incremental = false
codegen-units = 1
lto = true
