[Skipped]
# These tests have race conditions or extreme slowness on WSL
input/cookie.html = Flaky storage test - passes individually but fails in parallel execution
input/localStorage.html = Flaky storage test - passes individually but fails in parallel execution
input/HTML/Window-postMessage.html = Extremely slow on WSL (220+ seconds), times out even with high timeout
