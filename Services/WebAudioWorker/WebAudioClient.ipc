#include <LibWeb/WebAudio/AudioParamDescriptorIPC.h>

endpoint WebAudioClient
{
	webaudio_session_worklet_processor_error(u64 session_id, u64 node_id) => ()
	webaudio_session_worklet_processor_registered(u64 session_id, String name, Vector<Web::WebAudio::AudioParamDescriptor> descriptors, u64 generation) => ()
}
