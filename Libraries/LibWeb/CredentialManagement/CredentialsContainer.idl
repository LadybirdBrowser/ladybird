[Exposed=Window, SecureContext]
interface CredentialsContainer {
    [NeedsCallerRealm] Promise<Credential?> get(optional CredentialRequestOptions options = {});
    [NeedsCallerRealm] Promise<undefined> store(Credential credential);
    [NeedsCallerRealm] Promise<Credential?> create(optional CredentialCreationOptions options = {});
    [NeedsCallerRealm] Promise<undefined> preventSilentAccess();
};

dictionary CredentialRequestOptions {
    CredentialMediationRequirement mediation = "optional";
    AbortSignal signal;
};

dictionary CredentialCreationOptions {
    CredentialMediationRequirement mediation = "optional";
    AbortSignal signal;
};
