<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Inset so the full symbol fits inside the adaptive icon safe zone (~66dp of 108dp). -->
    <item
        android:left="22dp"
        android:top="22dp"
        android:right="22dp"
        android:bottom="22dp">
        <bitmap
            android:gravity="fill"
            android:src="@drawable/ladybird_logo" />
    </item>
</layer-list>
